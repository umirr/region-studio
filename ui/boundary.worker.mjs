import {edgeMap,selectBounded,smoothMask,cutNegativeToBoundary,reconcileAnchors} from './boundary.mjs';
let edge,w,h;
self.onmessage=({data:m})=>{try{if(m.type==='init'){w=m.width;h=m.height;edge=edgeMap(new Uint8ClampedArray(m.pixels),w,h);self.postMessage({request:m.request,ok:true});return;}if(!edge)throw Error('이미지 경계가 준비되지 않았습니다.');let result;if(m.type==='smooth')result={mask:smoothMask(m.mask,w,h,m.points,edge,m.amount)};else if(m.type==='negative'){let mask=m.mask,removed=0;for(const point of m.points){const cut=cutNegativeToBoundary(mask,w,h,point,edge,{...m.options,protectedPoints:m.protectedPoints});mask=cut.mask;removed+=cut.removed;}result={mask,removed};}else if(m.type==='reconcile')result=reconcileAnchors(m.mask,w,h,m.points,edge,m.options);else result=selectBounded(m.mask,w,h,m.points,edge,m.options);self.postMessage({request:m.request,...result},[result.mask.buffer]);}catch(e){self.postMessage({request:m.request,error:e.message});}};
